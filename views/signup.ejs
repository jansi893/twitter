<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-screen flex justify-center items-center bg-gradient-to-r from-purple-500 via-pink-500 to-yellow-400">

    <div class="max-w-md w-full bg-white/30 backdrop-blur-lg p-8 rounded-xl shadow-xl border border-white/20">
        <h2 class="text-4xl font-bold text-center text-white drop-shadow-lg">Signup</h2>
        <p class="text-white text-center text-lg mt-2 mb-6">Create a new account</p>

        <form action="/signup" method="POST">
            <div class="mb-4">
                <input type="email" name="email" placeholder="Email"
                    class="w-full px-4 py-3 rounded-lg border-2 border-white/50 bg-white/30 placeholder-white text-white focus:outline-none focus:ring-2 focus:ring-yellow-400">
            </div>

            <div class="mb-4">
                <input type="text" name="username" placeholder="Username"
                    class="w-full px-4 py-3 rounded-lg border-2 border-white/50 bg-white/30 placeholder-white text-white focus:outline-none focus:ring-2 focus:ring-yellow-400">
            </div>

            <div class="mb-4">
                <input type="password" name="password" placeholder="Password"
                    class="w-full px-4 py-3 rounded-lg border-2 border-white/50 bg-white/30 placeholder-white text-white focus:outline-none focus:ring-2 focus:ring-yellow-400">
            </div>

            <button type="submit"
                class="w-full py-3 rounded-lg bg-gradient-to-r from-green-400 to-blue-500 text-white font-bold text-lg hover:scale-105 transition-all">
                Sign Up
            </button>
        </form>

        <div class="flex justify-between items-center mt-4">
            <p class="text-white/80 text-sm">Already have an account?</p>
            <a href="/" class="text-white/80 text-sm hover:text-white transition">Login</a>
        </div>
    </div>

</body>
</html>